#include <cmath>
#include <cstdio>
#include <iostream>
#include <regex>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::getline;
using std::vector;

enum Node {
	tag,
	label,
	value
}

int main() {

	// Get meta data
	int N, Q;
	cin >> N >> Q;

	
	string line, lines;

	getline(cin, line);

	for (int i = 0; i < N; i++) {

	}

}